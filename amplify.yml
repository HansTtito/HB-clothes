version: 1
frontend:
  phases:
    build:
      commands:
        - echo "HB clothes - sitio estatico, no requiere build"
  artifacts:
    baseDirectory: /
    files:
      - "**/*"
  cache:
    paths: []
